import React from 'react';

const BlogForm = ({ initialData, onSubmit }) => {
  // TODO: useState for form fields, handleChange, handleSubmit
  return (
    <form>
      {/* TODO: Add form fields for title, content, category, tags, etc. */}
      <button type="submit">Save</button>
    </form>
  );
};

export default BlogForm; 